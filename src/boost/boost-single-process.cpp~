#include <iostream>
#include <string> 

#include <boost/interprocess/sync/named_mutex.hpp>



int main()
{

  // Create inteprocess mutex Object 
  bi::named_mutex m1(bi::open_or_create, "logger_mutex");

  bool isLocked = m1.try_lock();
  if(isLocked)

  return 0;
}
